import { StyleSheet } from "react-native";
import { COR_FUNDO_CARTELA, COR_LEGENDA, COR_MILIONARIA } from "../constants/Cores";
import { estatistica } from "../utils/ultil";

export const STYLES = StyleSheet.create({


    itemPremiacao: {
        backgroundColor: COR_LEGENDA,
        borderWidth: 1,
        width: "95%",
        padding: 10,
        flexDirection: "row",
        marginVertical: 1,
        borderRadius: 15,
        justifyContent: "center"

    },

   
})